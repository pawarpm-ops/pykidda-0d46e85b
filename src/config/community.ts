// Centralized community/contact URLs.
// Set to a real URL to enable the corresponding card; leave null to render as disabled.
export const COMMUNITY = {
  whatsapp: null as string | null,
  telegram: null as string | null,
  instagram: null as string | null,
  contactEmail: "pykidda@gmail.com" as string,
  contactPhone: "+919172504205" as string,
};
